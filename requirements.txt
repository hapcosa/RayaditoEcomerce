# Piedras Rayadito — dependencias backend (solo top-level; pip resuelve transitivas).
# Python 3.12. Ver docs/ROADMAP.md (Fase 0).

# --- Core ---
Django>=5.2,<5.3
psycopg[binary]>=3.2,<4

# --- API / Auth ---
djangorestframework>=3.16,<4
djangorestframework-simplejwt>=5.4,<6
djoser>=2.3,<3
social-auth-app-django>=5.4,<6
drf-spectacular>=0.27,<1

# --- Utilidades Django ---
django-cors-headers>=4.4,<5
django-environ>=0.11,<1
django-ckeditor-5>=0.2.13
django-storages>=1.14,<2
django-starken==1.0.7
Pillow>=10.4,<12
whitenoise>=6.6,<7

# --- Pagos / HTTP ---
mercadopago>=2.2,<3
requests>=2.32,<3

# --- Servidor ---
gunicorn>=23,<24

# --- Dev / formato ---
black>=24.10
